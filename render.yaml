services:
  - type: web
    name: ai-tutor-screener
    env: python
    plan: free
    rootDir: backend
    buildCommand: pip install uv && uv sync
    startCommand: uv run uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: GROQ_API_KEY
        sync: false
      - key: CONVERSATION_MODEL
        value: openai/gpt-oss-120b
      - key: ASSESSMENT_MODEL
        value: openai/gpt-oss-120b
